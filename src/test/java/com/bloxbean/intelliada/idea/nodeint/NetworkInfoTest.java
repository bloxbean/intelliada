package com.bloxbean.intelliada.idea.nodeint;

//import akka.actor.ActorSystem;
//import iog.psg.cardano.CardanoApiCodec;
//import iog.psg.cardano.jpi.CardanoApi;
//import iog.psg.cardano.jpi.CardanoApiBuilder;
import org.junit.Test;

import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class NetworkInfoTest {

    @Test
    public void testNetworkInfo() throws Exception {
//        ActorSystem as = ActorSystem.create();
//        ExecutorService es = Executors.newFixedThreadPool(1);
//        CardanoApiBuilder builder =
//                CardanoApiBuilder.create("http://cardano-test1:8090/v2/")
//                        .withActorSystem(as) // <- ActorSystem optional
//                        .withExecutorService(es); // <- ExecutorService optional
//
//        CardanoApi api = builder.build();
//        CardanoApiCodec.NetworkInfo networkInfo = api.networkInfo().toCompletableFuture().get();
//
//        System.out.println(networkInfo.toString());
    }
}
